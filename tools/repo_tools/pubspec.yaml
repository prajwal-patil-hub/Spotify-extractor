name: repo_tools
description: >-
  Workspace tooling: the architecture boundary checker that enforces the
  dependency rules in boundaries.yaml (docs/04).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

executables:
  check_boundaries:

dependencies:
  yaml: ^3.1.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
