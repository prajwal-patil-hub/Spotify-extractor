name: core_domain
description: >-
  Bridgetune domain core: entities, identifiers, capability model, confidence
  tiers, and the typed error hierarchy. Pure Dart — this package must never
  depend on Flutter, on any provider package, or on any infrastructure.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  collection: ^1.19.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
