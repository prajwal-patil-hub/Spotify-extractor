name: provider_spotify
description: >-
  Spotify plugin: implements the MusicProvider port against the Spotify Web
  API with OAuth 2.0 + PKCE. No Spotify knowledge exists outside this package.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  core_domain: ^0.1.0
  dio: ^5.7.0
  provider_api: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
  testing_toolkit: ^0.1.0
