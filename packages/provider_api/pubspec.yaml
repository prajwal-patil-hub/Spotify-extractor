name: provider_api
description: >-
  The MusicProvider port every streaming-service plugin implements, plus the
  shared auth substrate (PKCE, AuthBroker, TokenStore) and the per-provider
  rate governor. Pure Dart.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  core_domain: ^0.1.0
  crypto: ^3.0.6
  dio: ^5.7.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
