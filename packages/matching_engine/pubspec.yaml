name: matching_engine
description: >-
  The song matching engine (docs/07): normalization with variant flags,
  multi-signal fuzzy scoring, the query ladder, confidence tiers, and the
  margin rule. Pure Dart, zero provider knowledge.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  core_domain: ^0.1.0
  meta: ^1.16.0
  provider_api: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
